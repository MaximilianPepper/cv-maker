import React from "react";
import { Button } from "@mui/material";

const ButtonsHeader = () => {
  return (
    <div>
      <Button variant="outlined">Clear</Button>
      <Button variant="outlined">Fill</Button>
    </div>
  );
};

export default ButtonsHeader;
